<.flash_group flash={@flash} />

<%= for cate <- @cates do %>
    <li>
      <.link href={~p"/#{cate.name}"}><%= cate.name %></.link>
    </li>
    <p>Total posts:<%= length(cate |> Ecto.assoc(:posts) |> Twitter.Repo.all) %></p>
<% end %>